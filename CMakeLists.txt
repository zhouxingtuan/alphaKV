CMAKE_MINIMUM_REQUIRED(VERSION 3.0)
PROJECT(alphaKV)

SET(FLAGS "-std=c++11 -O3")
IF (ANDROID)
    SET(FLAGS "${FLAGS} -pie -fPIE")
ENDIF()
SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${FLAGS}")
SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${FLAGS}")

ADD_EXECUTABLE(main main.cpp)